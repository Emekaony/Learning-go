package main

import (
	"fmt"
	"math"
)

func main() {
	// just a basic string
	var a = "first"
	// could also do something like
	p := "pushin p"
	fmt.Println(p)
	var d, b, c int = 1, 2, 3
	fmt.Println(a)
	fmt.Println(d, b, c)

	const n int64 = 1000
	const m = n / 5
	println(m)
	println(math.Cos(0))
}
